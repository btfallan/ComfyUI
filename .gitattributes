# Auto detect text files and perform LF normalization. Set the default behavior, in case people don't have core.autocrlf set.
# * text=auto
# files that will always have LF line endings on checkout.
*.sh text eol=lf
ComfyUI_Workflows/**/*.png filter=lfs diff=lfs merge=lfs -text
